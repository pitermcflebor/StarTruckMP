# How to deploy with Docker

## Prerequisites

- Docker installed on your host
- Download from releases the zip file for docker hosting

## Running the server

1. Unzip the downloaded file
1. Open a terminal on the unzipped folder
1. Run the following command to start the server:
   * `docker compose up -d --build`
1. The server will be available at `your-server-ip:7777`